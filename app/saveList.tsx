import SaveList from "../compoment/screen/SaveList"
import { SafeAreaView } from "react-native"

const saveList = () => {
  return(
    <SafeAreaView style={{flex:1}}>
      <SaveList/>
    </SafeAreaView>
  )
}

export default saveList